<div class='body'>
            <%= flash[:notice] %>
  <div class='container'>
    <p id="notice"></p>
    <div class='row'>
      <div class='col-xs-3'>
        <h2>User info</h2>
        <table class='table'>
          <tr><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%></tr>
          <tr>
    	      <th>name</th>
    	      <th><%= @books.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= @books.introduction %></th>
          </tr>
        </table>

        <div class='row'>
          <%= link_to 'edit', edit_user_path(current_user.id) %>
        </div>
        <h2>New book</h2>
        <%= form_with model:@book, local:true do |f| %>
            <div class="field row">
              <label for="book_title">Title</label><br />
              <td><%= f.text_field :title, :size => "40*40"  %></td>
            </div>

            <div class="field row">
              <label for="book_opinion">Opinion</label><br />
              <td><%= f.text_area :body, :size => "39*39" %></td>
            </div>

            <div class="actions row">
              <%= f.submit 'Create Book' %>
            </div>
        <% end %>
      </div>

      <div class='col-xs-9'>
		    <h2>Book detail</h2>
		    <table class='table'>
		      <tr>

            <td>
              <%= attachment_image_tag @user, :profile_image, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg", size: "40x40" %><br>
              <% @user.name %>
            </td>
            <td><%= @books.title %></td>
            <td><%= @books.body %></td>

            <td><%= link_to 'Edits', edit_book_path(books) %></td>
            <td>Destroy</td>
          </tr>
        </table>
      </div>
    </div>
  </div>
</div>
